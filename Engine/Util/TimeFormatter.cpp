#include "TimeFormatter.h"
#include <chrono>
/*
namespace Mithril
{
	std::string Mithril::TimeFormatter::GetDayTime()
	{
		char timestamp[50]{ 0 };
		std::time_t time = std::time(nullptr);
		//std::strftime(timestamp, 30, "[%H:%M:%S]", std::localtime(&time));
	}
}
*/